{
  "name": "pi-switch",
  "version": "0.1.0",
  "description": "Multi-account provider switching for Pi — manage multiple API keys and custom providers per provider, with /switch and /switch models.",
  "type": "module",
  "main": "./dist/index.js",
  "keywords": [
    "pi-package",
    "pi",
    "pi-coding-agent",
    "extension",
    "multi-account",
    "provider",
    "switch"
  ],
  "license": "MIT",
  "files": [
    "dist/**/*.js",
    "README.md",
    "LICENSE"
  ],
  "pi": {
    "extensions": [
      "./dist/index.js"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-agent-core": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.83.0",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist"
  }
}
